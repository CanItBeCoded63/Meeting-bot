
# Client Package

This folder contains the client-side package for this project.

## Setup

1. Use the workspace setup steps in the root README.
2. Configure environment variables in the root `.env` file.
3. Use the root scripts and commands to run the project.

## Notes

- Keep package-specific changes inside this folder.
- Keep shared model changes in the `common` package.
